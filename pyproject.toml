[tool.ruff]
target-version = "py313"
line-length = 100

[tool.ruff.lint]
# E/F = pycodestyle + pyflakes (core correctness), I = import sorting.
# E501 (line too long) is left to the formatter, not enforced as a lint error.
select = ["E", "F", "I"]
ignore = ["E501"]
